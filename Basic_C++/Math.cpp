#include "Math.h"

void Basic()
{
	ceil(1.6);		// "=2", 올림
	round(1.6);		// "=2", 반올림
	round(1.3);		// "=1", 반올림
	floor(1.3);		// "=1", 내림

	abs(-10);		// "=10", 절대값
	fabs(-10.4);	// "=10", 절대값
	pow(3, 4);		// "=81", 제곱
	sqrt(81);		// "=9", 제곱근


}